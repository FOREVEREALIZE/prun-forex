services:
  forex:
    build: .
    image: ghcr.io/foreverealize/prun-forex:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - forex-data:/data

volumes:
  forex-data:
